export function add(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}

export default function multiply(a, b) {
  return a * b;
}

//CJS modules export
// module.exports = {
//   add,
//   sub,
// };

//ES modules exports
// export { add, sub };
